require 'spec_helper'

describe CsvHasher do
  it 'has a version number' do
    expect(CsvHasher::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
